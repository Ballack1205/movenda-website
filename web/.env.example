# Copy to .env when running a host or theme locally. Defaults = QR preview (navy, full site, noindex).

# PUBLIC_NOINDEX=false   # only after Friday 25 Sep 2026 + explicit OK, and only on the intended host
# PUBLIC_SITE_URL=https://movenda-preview.onrender.com
# PUBLIC_THEME=current   # or lab
# PUBLIC_HOST_MODE=full  # or mpc
# PUBLIC_MOVENDA_URL=https://www.movenda.be
