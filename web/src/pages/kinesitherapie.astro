---
import Layout from "../layouts/Layout.astro";
import GoogleReviewBadge from "../components/GoogleReviewBadge.astro";
import { getLocatieBySlug, getTeamledenByLocatie } from "../lib/content";
import { breadcrumbSchema, localBusinessSchema } from "../lib/schema";

const locatie = (await getLocatieBySlug("olympia"))!;
const team = await getTeamledenByLocatie("olympia");

const klachten = [
  "Rug- en nekklachten",
  "Sportblessures & sportrevalidatie",
  "Postoperatieve revalidatie",
  "Pre- en postnatale klachten",
  "Cardiovasculaire revalidatie",
  "Chronische pijn",
];

const behandelingen = [
  { naam: "Manuele therapie", tekst: "Hands-on technieken om gewrichten en spieren te mobiliseren." },
  { naam: "Oefentherapie", tekst: "Actieve, opbouwende oefeningen die je zelf verder zet." },
  { naam: "Pre- en postnatale kinesitherapie", tekst: "Begeleiding voor, tijdens en na de zwangerschap." },
  { naam: "Lymfedrainage", tekst: "Manuele drainage bij zwelling of vochtophoping." },
  { naam: "Dry needling", tekst: "Naaldtherapie tegen spanning en triggerpoints." },
  { naam: "Acupunctuur", tekst: "Traditionele naaldtechniek, aanvullend op kinesitherapie." },
  { naam: "Auriculotherapie", tekst: "Acupunctuur via specifieke punten op het oor." },
  { naam: "Cupping", tekst: "Cuptherapie ter ondersteuning van spierherstel en doorbloeding." },
  { naam: "Taping", tekst: "Ondersteunende tape bij overbelasting of instabiliteit." },
];

const jsonLd = [
  localBusinessSchema(locatie),
  breadcrumbSchema([
    { name: "Home", path: "/" },
    { name: "Kinesitherapie", path: "/kinesitherapie" },
  ]),
];
---

<Layout
  title="Kinesitherapie Hasselt | Movenda"
  description="Team van ervaren kinesisten in Hasselt: manuele therapie, dry needling, acupunctuur, pre- en postnatale kinesitherapie en meer."
  path="/kinesitherapie"
  jsonLd={jsonLd}
>
  <section class="mx-auto max-w-4xl px-4 py-12">
    <h1 class="text-3xl font-bold">Kinesitherapie Hasselt</h1>
    <p class="mt-3 max-w-2xl text-brand-muted">
      Bij Movenda in Hasselt vind je een team van {team.length}+ ervaren kinesisten die je
      begeleiden bij uiteenlopende spier- en gewrichtsklachten. Of je nu last hebt van rugpijn,
      herstelt na een blessure of een operatie, of gewoon pijnvrij wil bewegen — onze
      kinesitherapeuten stellen een behandeling op maat voor jou op.
    </p>
    <p class="mt-3 max-w-2xl text-brand-muted">
      Wij combineren klassieke kinesitherapie met de nieuwste technieken en inzichten. Elke
      behandeling duurt 30 minuten één-op-één, waarna je extra tijd krijgt om je oefeningen
      zelfstandig verder te zetten. Zo werk jij actief mee aan je eigen herstel.
    </p>
    <div class="mt-6 flex flex-wrap items-center gap-3">
      <a
        href={`tel:${locatie.telefoon.replace(/\s+/g, "")}`}
        class="rounded-full bg-brand-primary px-6 py-3 font-semibold text-white transition hover:bg-brand-primary-dark"
      >
        Bel {locatie.telefoon}
      </a>
      <GoogleReviewBadge locatie="olympia" />
    </div>

    <h2 class="mt-12 text-xl font-bold">Klachten die we behandelen</h2>
    <div class="mt-4 flex flex-wrap gap-2">
      {klachten.map((k) => (
        <span class="rounded-full border border-brand-border bg-brand-surface px-4 py-1.5 text-sm text-brand-text">
          {k}
        </span>
      ))}
    </div>

    <h2 class="mt-10 text-xl font-bold">Onze behandeltechnieken</h2>
    <div class="mt-4 grid gap-4 sm:grid-cols-2">
      {behandelingen.map((b) => (
        <div class="rounded-2xl border border-brand-border bg-brand-surface p-6">
          <h3 class="font-semibold">{b.naam}</h3>
          <p class="mt-1 text-sm text-brand-muted">{b.tekst}</p>
        </div>
      ))}
    </div>

    <p class="mt-8 text-sm text-brand-muted">
      Wij werken enkel op afspraak vanuit onze praktijk in Sportcentrum Olympia,{" "}
      {locatie.adres}.
    </p>
  </section>
</Layout>
